import styles from "./ContactUs.module.css"

const ContactUs = () => {
  return (
    <div className={`${styles.h1} container`}>
        <div>
            <h1>Contact Us</h1>
        </div>
    </div>
  )
};

export default ContactUs;